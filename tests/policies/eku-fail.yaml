id: integration-eku-fail
version: 1.0

rules:
  - id: leaf-must-have-client-auth
    reference: integration-test
    target: certificate
    operator: ekuContains
    operands: ["clientAuth"]
    certType: [leaf]
    severity: error
    message: "Leaf must contain clientAuth EKU (intentional mismatch — leaf has serverAuth only)"
